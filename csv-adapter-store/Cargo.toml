[package]
name = "csv-adapter-store"
version = "0.1.0"
edition = "2021"
description = "SQLite persistence layer for CSV Adapter seal and anchor storage"
license = "MIT"

[dependencies]
csv-adapter-core = { path = "../csv-adapter-core" }
rusqlite = { version = "0.30", features = ["bundled"] }
thiserror = "1.0"
